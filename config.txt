# Default configuration for A-Maze-ing
WIDTH=29
HEIGHT=12
ENTRY=0,0
EXIT=28,11
OUTPUT_FILE=maze.txt
PERFECT=True
ENABLE_TUI=False
SEED=42
# RATIO is used only when PERFECT=False.
# If PERFECT=True, the maze remains perfect (RATIO is ignored).
RATIO=0.1
